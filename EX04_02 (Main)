#include <iostream>
#include "Header.h"
using namespace std;

int* doubleCapacity(const int*list, int size);

int main() 
{
	int ArraySize;
	cin >> ArraySize;
	int* TheArray = new int[ArraySize];
	int *NewArray = doubleCapacity(TheArray, ArraySize);

	return 0;
}

