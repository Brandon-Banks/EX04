#include <iostream>
#include <cmath>
#include <string>
using namespace std;

int main()
{
	void prompt_count(int* size)
	{
		cout << "how large is the array? ";
		cin >> *size;
	}

	int* doubleCapacidty(const int* list, int size)
	{
		for (int* i = numbers; i < numbers + size; i++)
		{
			cout << "Enter a number: ";
			cin >> *i;
		}
	}

	int* alloc_array(int size)
	{
		int* pintarray = new int[size];

		for (int i = 0; i < size; i++)
		{
			pintarray[i] = 0;
		}
		return pintarray;
	}
}
